package com.translate.webtranslator;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The main class for the WebTranslator application. It launches the Spring Boot application.
 */
@SpringBootApplication
public class WebTranslatorApplication {

    public static void main(String[] args) {
		SpringApplication.run(WebTranslatorApplication.class, args);
	}
}